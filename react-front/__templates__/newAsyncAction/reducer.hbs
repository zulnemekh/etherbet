  [actions.{{camelCase action}}]: (state, action, meta) => ({
    ...state,
    loaders: {
      ...state.loaders,
      [constants.{{constantCase action}}]: true
    }
  }),
  [actions.{{camelCase action}}Complete]: (state, action, meta) => ({
    ...state,
    loaders: {
      ...state.loaders,
      [constants.{{constantCase action}}]: false
    }
  }),
  [actions.{{camelCase action}}Error]: (state, action, meta) => ({
    ...state,
    loaders: {
      ...state.loaders,
      [constants.{{constantCase action}}]: false
    },
    errors: {
      ...state.error,
      [constants.{{constantCase action}}]: action.payload.message
    }
  }),
/*__ADD_ACTION_HANDLER__*/